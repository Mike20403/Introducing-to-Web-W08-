<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>21120090</title>
  <link
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
  rel="stylesheet"
  integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
  crossorigin="anonymous"
  />
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-4">
        <div class="card">
          <div class="card-header">Calculator</div>
          <div class="card-body">
            <form action="/calculate" method="post">
              <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1">x</span>
                <input
                type="text"
                class="form-control"
                name="x"
                value="<%= xv ? xv : 0 %>"
                />
              </div>

              <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1">y</span>
                <input
                type="text"
                class="form-control"
                name="y"
                value="<%= yv ? yv : 0 %>"
                />
              </div>

              <select name="opt" class=" form-select">
                <option value="plus" selected>+</option>
                <option value="minus">-</option>
                <option value="multi">*</option>
                <option value="divide">/</option>
              </select>

              <button type="submit" class="btn btn-primary form-control my-2">
                Calculator
              </button>
            </form>

            <div  class="<%= result !== 'Cannot divide by zero!!' ? 'alert-success' : 'alert-danger' %>"  class="alert" role="alert"> <%= result %></div>
          </div>
        </div>
      </div>
    </div>
  </div>

</body>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

</html>
